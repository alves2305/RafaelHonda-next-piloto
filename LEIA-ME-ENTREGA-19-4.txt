ENTREGA 19.4 — PAINEL CONECTADO AOS DADOS REAIS

ANTES DE INSTALAR

Confirme a branch:

git branch --show-current

Resultado obrigatório:

feature/painel-clientes

Também confirme que a Entrega 19.3 foi enviada:

git status

O ideal é aparecer sem alterações pendentes.

INSTALAÇÃO

Extraia este ZIP dentro da pasta principal do projeto.

Arquivos:

lib/client-panel-data.ts
app/cliente-demo/dashboard/page.tsx
app/cliente-demo/dashboard/client-panel-data.module.css
docs/entrega-19-4-dados-reais.md

Nenhum SQL precisa ser executado nesta entrega.

TESTES

npm.cmd run typecheck
npm.cmd run lint
npm.cmd run build
npm.cmd run dev

Abra:

http://localhost:3000/cliente-demo/login

Entre com a conta de teste vinculada à GD Honda.

CONFIRME

1. nome real do vendedor;
2. slug correto;
3. WhatsApp real;
4. Instagram real;
5. slogan real;
6. cores reais;
7. modalidades reais;
8. quantidade real de motos;
9. lista real das motos;
10. links das motos abrindo o catálogo correto;
11. espaço de visitas e moto mais acessada aparecendo como “em preparação”;
12. logout funcionando.

IMPORTANTE

- os dados de perfil estão em modo somente leitura;
- a seleção de motos está em modo somente leitura;
- nenhuma permissão de escrita foi criada;
- pagamentos continuam simulados;
- métricas ainda não são registradas;
- não execute SQL;
- não faça merge na main;
- não crie tag.

COMMIT

Depois de aprovar no localhost:

git add .
git commit -m "Conecta painel do vendedor aos dados reais"
git push origin feature/painel-clientes
