ENTREGA 19.1.2 — CORREÇÃO DOS TRÊS ERROS DE LINT

O QUE ESTA CORREÇÃO FAZ

Corrige somente a regra react-hooks/set-state-in-effect nestes arquivos:

app/admin/dashboard/page.tsx
app/cliente-demo/admin-cobranca/page.tsx
app/cliente-demo/assinatura/page.tsx

O comportamento visual e funcional permanece igual.

ESTADO ATUAL DO PROCESSO

O script de restauração já realizou:

- backup emergencial;
- criação da documentação;
- alteração local da versão para 1.0.0;
- typecheck aprovado.

Ele parou no lint antes de:

- commit;
- tag;
- push;
- criação da branch.

Portanto, não é necessário desfazer nada.

INSTALAÇÃO

1. Extraia este ZIP dentro da pasta principal do projeto.
2. Confirme a substituição dos três arquivos.
3. Não apague docs, package.json ou package-lock.json.
4. Execute novamente:

powershell -ExecutionPolicy Bypass -File .\scripts\release\criar-ponto-restauracao-v1.0.0.ps1

O script criará um novo backup e continuará o processo completo.

TESTE MANUAL OPCIONAL

Antes do script completo, pode confirmar somente o lint:

npm run lint

Depois execute o script de restauração normalmente.
